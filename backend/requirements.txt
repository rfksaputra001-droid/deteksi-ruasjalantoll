# =============================================================================
# Requirements.txt - FastAPI + YOLO + OpenCV - Render Optimized  
# Updated for Python 3.13 compatibility and Render deployment
# =============================================================================

# === FastAPI & Server ===
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-socketio==5.10.0
aiofiles==23.2.1

# === Database ===
pymongo==4.6.0
motor==3.3.2

# === Authentication ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# === Configuration ===
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# === Image Processing & Computer Vision ===
# Use pre-compiled wheels for faster builds on Render
Pillow>=10.2.0
opencv-python-headless>=4.8.0
numpy>=1.24.0,<2.0.0

# === YOLO Detection ===
# Ultralytics YOLO with flexible versioning for compatibility
ultralytics>=8.0.0

# === PyTorch (CPU version - let pip resolve best versions) ===
torch>=2.0.0
torchvision>=0.15.0

# === Cloud Services ===
cloudinary>=1.36.0
requests>=2.31.0
httpx>=0.26.0

# === Utilities ===
python-dateutil>=2.8.2
typing-extensions>=4.8.0

# === Build tools for compatibility ===
setuptools>=65.0.0
wheel>=0.38.0