# =============================================================================
# Requirements.txt - FastAPI + YOLO + OpenCV
# Tested and compatible versions for Docker/Render deployment
# =============================================================================

# === FastAPI & Server ===
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-socketio==5.10.0
aiofiles==23.2.1

# === Database ===
pymongo==4.6.0
motor==3.3.2

# === Authentication ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# === Configuration ===
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# === Image Processing & Computer Vision ===
# Updated Pillow for Python 3.13 compatibility 
Pillow>=10.2.0
# OpenCV headless - NO GUI dependencies required
# This version is tested to work with Python 3.11+ on Debian/Ubuntu
opencv-python-headless==4.9.0.80
# NumPy - compatible version for OpenCV and PyTorch
numpy>=1.24.0,<2.0.0
# Additional image processing - updated versions
scipy>=1.11.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.8

# === YOLO Detection ===
# Ultralytics YOLO - includes all detection dependencies, updated for Python 3.13
ultralytics>=8.1.0

# === PyTorch (CPU version for smaller image size) ===
# Updated PyTorch for Python 3.13 compatibility
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0+cpu
torchvision>=0.16.0+cpu
torchaudio>=2.1.0+cpu

# === Cloud Services ===
cloudinary==1.36.0
requests==2.31.0
httpx==0.26.0

# === Utilities ===
python-dateutil==2.8.2
typing-extensions==4.9.0
psutil==5.9.6